package com.company;


import static java.lang.Character.isUpperCase;

public class Question_3 {
    public static void count(String str) {
        int len = str.length();
        char temp;
        int count;
        for (int i = 0; i < len; i++) {
            temp = str.charAt(i);
            boolean a = isUpperCase(temp);
// Comment
        }
    }
}
